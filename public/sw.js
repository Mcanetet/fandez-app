/* Bootstrap: el CDN de Hostinger puede servir este archivo sin headers.
 * El service worker real vive en /service-worker.js (ruta Node). */
/* eslint-disable no-undef */
importScripts('/service-worker.js');
